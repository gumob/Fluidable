//
//  EnumSpec.swift
//  FluidableTests
//
//  Created by kojirof on 2019/06/29.
//  Copyright © 2019 Gumob. All rights reserved.
//

import Quick
import Nimble
@testable import Fluidable

class EnumSpec: QuickSpec {

    override func spec() {
        describe("Spec FluidablePosition") {
//            describe("Spec FluidablePosition") {
//                it("is equal") {
//                    let top: FluidDrawerPosition = FluidDrawerPosition.top
//                    let bottom: FluidDrawerPosition = FluidDrawerPosition.bottom
//                    expect(top.opposite).to(equal(bottom))
//                    expect(bottom.opposite).to(equal(top))
//                }
//            }
//            describe("Spec FluidableState") {
//                it("is equal") {
//                    expect(FluidableState.initial.description).to(equal("initial"))
//                    expect(FluidableState.pulling.description).to(equal("pulling"))
//                    expect(FluidableState.overThreshold.description).to(equal("overThreshold"))
//                    expect(FluidableState.loading.description).to(equal("loading"))
//                    expect(FluidableState.finished.description).to(equal("finished"))
//                    expect(FluidableState.backing.description).to(equal("backing"))
//
//                    expect(FluidableState.initial).to(equal(.initial))
//                    expect(FluidableState.pulling).to(equal(.pulling))
//                    expect(FluidableState.overThreshold).to(equal(.overThreshold))
//                    expect(FluidableState.loading).to(equal(.loading))
//                    expect(FluidableState.finished).to(equal(.finished))
//                    expect(FluidableState.backing).to(equal(.backing))
//                }
//                it("is not equal") {
//                    expect(FluidableState.initial).notTo(equal(.pulling))
//                    expect(FluidableState.initial).notTo(equal(.overThreshold))
//                    expect(FluidableState.initial).notTo(equal(.loading))
//                    expect(FluidableState.initial).notTo(equal(.finished))
//                    expect(FluidableState.initial).notTo(equal(.backing))
//                }
//            }
//            describe("Spec FluidableAnimationType") {
//                it("is equal") {
//                    let linearType: FluidableAnimationType = .linear(duration: 1.0)
//                    let springType: FluidableAnimationType = .spring(duration: 1.0)
//                    let customType: FluidableAnimationType = .custom(duration: 1.0,
//                                                                           delay: 0.0,
//                                                                           springDamping: 0.42,
//                                                                           initialSpringVelocity: 0.8,
//                                                                           options: [.curveLinear])
//                    let linear: FluidableAnimationOption = FluidableAnimationOption(1.0, 0.0, 1.0, 0.2, [.curveLinear])
//                    let spring: FluidableAnimationOption = FluidableAnimationOption(1.0, 0.0, 0.4, 0.8, [.curveLinear])
//                    let custom: FluidableAnimationOption = FluidableAnimationOption(1.0, 0.0, 0.4, 0.8, [.curveLinear])
//                    expect(linearType.option.duration).to(equal(linear.duration))
//                    expect(springType.option.duration).to(equal(spring.duration))
//                    expect(customType.option.duration).to(equal(custom.duration))
//                }
//            }
        }
    }

}
